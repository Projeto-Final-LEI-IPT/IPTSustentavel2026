import { registerRootComponent } from 'expo';
import App from './App';

// O registerRootComponent garante que, o ponto de entrada aponta sempre
// corretamente para o componente principal (App.js).
registerRootComponent(App);
